{
  "routes": [
    {
      "src": "/api/auth/(.*)",
      "dest": "/api/auth/$1",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"],
      "headers": {
        "Access-Control-Allow-Origin": "*"
      }
    }
  ],
  "functions": {
    "/api/.*": {
      "maxDuration": 60
    }
  }
}
